#include<stdio.h>

int main()
{
    char *str;   
    char *secret="enc0re{-This_is_the_address_to_flag}";
    scanf("%s",str);
    puts("aight imma just printf it again");
    printf(str);
    puts("\nexiting xD");
    exit(1);
}
